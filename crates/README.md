# Rust Crates

This directory is the future Rust workspace for CBN. The initial skeleton keeps
crates intentionally small so package boundaries are visible before the runtime
implementation is migrated from the Python experiment.

