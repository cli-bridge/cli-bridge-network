# @cli-bridge/adapter-cli-anything

Adapter boundary for CLI-Anything / CLI-Hub. This package should call CBN's
external plugin manager and `cli-hub` APIs; it must not vendor upstream
CLI-Anything source.

