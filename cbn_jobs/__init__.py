"""ERC-8183 Job Commerce — offchain Job store + state machine + CLI.

See runtime/erc8183-job-commerce-plan.md. Three temporary roles per job
(client / provider / evaluator); rejected is terminal (use revision).
"""
