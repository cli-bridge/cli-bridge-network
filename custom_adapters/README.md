# Custom Adapters

This directory mirrors ComfyUI's `custom_nodes` extension seam, adapted for
CBN. Future adapter packs can register capability manifests, route fragments,
workflow templates, and parser fixtures here during local development.

